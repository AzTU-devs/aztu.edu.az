import PartnerInstitutionPage from "@/components/about/PartnerInstitutionPage";

/** Backend-driven (About CMS, template "partner-institution"). The hero video
 *  is the section default; every other field comes from the dashboard. */
export default function Page() {
    return <PartnerInstitutionPage pageKey="baku-state-college-of-communication-and-transport" />;
}
